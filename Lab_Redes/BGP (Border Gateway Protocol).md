# P06. Border Gateway Protocol (BGP)


1. Lo primero que hacemos para resolver esta práctica es el siguiente comando:
	- `vtysh`
	- `configure`
	- `router bgp <100>`
	-  `neighbor  <ip-address> remote-as <as-numbe>`
	
   Repetimos los pasos con todas las combinaciones que nos da la práctica. 